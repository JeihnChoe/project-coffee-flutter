import 'package:flutter/material.dart';

class NearStoresPage extends StatelessWidget {
  const NearStoresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        SizedBox(child: Text("으악")),
      ],
    );
  }
}
